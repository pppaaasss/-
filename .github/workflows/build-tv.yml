name: Rebuild APTV playlists

on:
  push:
    paths:
      - 'scripts/build_playlist.py'
      - 'tests/test_build_playlist.py'
      - '.github/workflows/build-tv.yml'
  schedule:
    # Every four hours, offset from the top-of-hour Actions rush.
    - cron: '17 */4 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: rebuild-aptv-playlists
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
      - name: Run offline tests
        run: python3 -m unittest discover -s tests -v
      - name: Probe and build playlists
        id: playlist_build
        continue-on-error: ${{ github.event_name == 'push' && contains(github.event.head_commit.message, '[frame-audit]') }}
        env:
          TARGET_STABLE: '800'
          MIN_STABLE: '760'
          TARGET_ALL: '1000'
          TARGET_EASY: '200'
          MIN_EASY: '55'
          SOURCE_WORKERS: '12'
          PROBE_WORKERS: '56'
          PROBE_TIMEOUT: '8'
        run: python3 scripts/build_playlist.py
      - name: Install frame-audit image dependency
        if: github.event_name == 'push' && contains(github.event.head_commit.message, '[frame-audit]')
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg
          python3 -m pip install --quiet pillow
      - name: Capture CCTV and satellite frames for identity review
        if: github.event_name == 'push' && contains(github.event.head_commit.message, '[frame-audit]')
        run: python3 scripts/audit_tv_frames.py --playlist tv-easy.m3u --output frame-audit --workers 8 --timeout 30 || true
      - name: Upload family-channel frame audit
        if: github.event_name == 'push' && contains(github.event.head_commit.message, '[frame-audit]')
        uses: actions/upload-artifact@v4
        with:
          name: family-channel-frame-audit
          path: frame-audit/
          retention-days: 3
      - name: Propagate playlist build failure after audit upload
        if: steps.playlist_build.outcome == 'failure'
        run: exit 1
      - name: Publish refreshed playlists
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tv-easy.m3u tv.m3u tv-all.m3u health-history.json build-report.txt
          if git diff --cached --quiet; then
            echo 'No playlist changes'
          else
            git commit -m 'Refresh measured APTV playlists'
            git push
          fi
