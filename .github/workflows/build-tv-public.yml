name: Legacy static IPTV builder (disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow notice
        run: 'echo "Disabled: use Rebuild APTV playlists instead."'
