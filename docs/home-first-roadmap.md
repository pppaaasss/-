# 家庭优先电视健康系统迁移路线图

> 2026-09-05 已获用户确认，通过 [PR #5](https://github.com/pppaaasss/-/pull/5) 合入 `master`（`ae0c5fdd59b20c41e40cb61177bb15d364f58d36`）。当前断点：等待用户把 U 盘插入 AC86U，下一步仅只读确认磁盘、挂载及路由器环境。发布器仍关闭、编号为空，尚未安装探针或进行真实影子验收。下方“待合并”表述若属于历史记录，不再作为当前待办。

> 当前工作分支：`home-first-ac86u`
>
> 起点提交：`ceeb4d7f6b9206811d8ae7c193930182409a3094`
>
> 最终原则：GitHub 只搜集和发布；AC86U 从家庭实际分流路径检测并拥有唯一裁决权；香港主机不参与检测、中转或替换。

## 2026-09-05 实施更新（优先于下方历史阶段记录）

- 开工时 master：`8ddfac295a76176f2b8b5be7f1dfe0d0ff95e8f3`；功能起点：`b6b348255ee2124085ee0de68cc26911af06de70`。
- 已在功能分支合入该 master，合并提交：`4643fd9887ad17e6846814502e3ebb4c75de3d16`。四份台单和采集候选逐字节保留 master。
- 额度中断后 master 继续推进，已再次合入 `c6fea1089af6730dbaf4323739d6b7fc258bf7e3`，同步提交 `647b89c4ac0b3915618bba98b835e406f2017d3a`；重新验证保留其最新台单与候选。
- 用户新确认：13:00 **不测试任何备用**，需要替换时只用未过期的凌晨家庭验证证据。已更新探针、报告契约和发布器；不恢复云端检测。
- 修复影子配对依赖正式发布启用的死循环；安装器补装 `activate.sh`；关闭发布器时支持只读影子校验。
- 修复 UNKNOWN 连续出现被转成 BAD；明确网络失败与检测未知分开，观看反馈进入家庭裁决。
- 凌晨优先正式与故障备用，再处理普通候选；检测预算收紧为 1200 秒，队列跨天续跑，首轮建池不再发空增量。
- 影子验收按不同报告的生成时间计数与跨度，重复推送不凑数。
- 新增 12 项部署回归验证。全仓 222 项：205 通过、6 失败、11 报错，17 项与起点逐项一致；家庭专项 80 项全部通过。
- YAML/Shell/台单结构通过，旧 easy 的重复深圳卫视记录仅标记，未修改。
- `enabled=false`、`expected_probe_id=""` 保持；已获用户确认并通过 PR #5 合入 master，尚未操作路由器。
- 完整结果、已知现场问题和后续步骤见 [`work-review-2026-09-05.md`](work-review-2026-09-05.md)。

下方阶段 0–7 的测试数量与实现描述是迁移历史快照；发生冲突时按本次更新和用户最新指令执行。

## 已确认的运行规则

- 所有时间均按家里路由器的北京时间（`Asia/Shanghai`）。
- 每天 `02:00` 执行主检：拉取 GitHub 当日新增/变更候选，检测候选能否成为家庭备用，同时检查全部央视和省级卫视正式线路并按需替换。
- 每天 `13:00` 执行复检：只检查央视和省级卫视正式线路；需要替换时使用有效的凌晨家庭合格备用，下午不测试备用。
- 废除每 6 小时巡检、每 72 小时深检和每 3 天统一轮换。线路合格就保持，不为轮换而轮换。
- 没有频道替换数量上限；同一轮不合格多少台，就处理多少台。
- GitHub 抓到的线路只能进入“候选池”，不得直接成为正式或备用线路。
- 只有 AC86U 从与 Apple TV 等价的 DNS、出口、分流和 IPv4/IPv6 路径验证通过后，候选才能进入“家庭合格备用池”。
- 香港的成功或失败证据权重均为零。
- 家庭断网、GitHub 不可达、报告不完整、路径未经确认、路由器资源不足或出现大面积同时失败时，保持上一份正式清单，禁止替换。
- 用户在电视上确认的卡顿、模糊、串台或错误频道，拥有最高优先级并可否决自动结果。

## 数据边界

| 数据 | 产生位置 | 含义 | 能否触发替换 |
|---|---|---|---|
| GitHub 候选池 | GitHub Actions | 新搜集、已分类和去重，但未经家庭验证 | 否 |
| 家庭合格备用池 | AC86U | 在家庭实际路径通过连续分片和质量检测 | 可以：凌晨本轮验证；下午只引用有效的凌晨证据 |
| 家庭正式线路报告 | AC86U | 当前央视和省级卫视在家中的真实状态 | 可以 |
| 用户反馈 | 家中实际观看 | 卡顿、清晰度和频道身份的最终证据 | 可以，优先级最高 |
| 香港检测报告 | 香港主机 | 与家庭使用目标无关 | 永远不可以 |

## 分阶段实施

每阶段必须满足：独立提交、测试结果写入本文件、能够安全停在该阶段、不得在验证前改动四份正式播放清单。

### 0. 基线审计与断点建立（已完成）

- [x] 从 GitHub 最新 `master` 建立隔离分支 `home-first-ac86u`。
- [x] 找出旧链路中的每 6 小时、72 小时、3 天轮换、香港探测和香港接收器入口。
- [x] 记录改动前测试基线。
- [x] 提交本路线图，作为后续额度中断后的唯一续做入口。

基线测试命令：`python3 -m unittest discover -s tests -v`

基线结果：共 163 项，146 项通过、6 项失败、11 项报错。失败均已存在于起点提交；主要来自旧播放清单构建断言、过期的 CCTV-5 修复测试和一个已被安全策略禁止的未核验导入器。本迁移不得增加新的失败，并以家庭探针、报告、发布及替换相关测试全绿为合并门槛。

### 1. 候选池与家庭备用池契约（已完成）

- [x] 定义版本化的“GitHub 未验证候选清单”格式，只保留央视和省级卫视需要的字段。
- [x] 定义版本化的“家庭合格备用池”和正式线路检测报告格式。
- [x] 把来源引用、频道规范名、URL 指纹、抓取时间和清单版本绑定在报告中。
- [x] 明确 `UNKNOWN` 永不等价于失败，且任何未知状态都不能触发生产变更。

阶段测试：新增 6 项契约测试全部通过；家庭探针、报告、旧轮换和家庭反馈相关 44 项测试全部通过。全仓库现在共 169 项，152 项通过、仍为原有的 6 项失败和 11 项报错，没有新增回归。使用当前累计候选做离线构建得到 53 个正式频道、1540 条未验证候选；后续采集阶段会改为每日增量，AC86U 不会一次吞下累计池。

### 2. AC86U 两时段家庭探针（已完成）

- [x] 将安装计划改为 `02:00` 主检和 `13:00` 复检两条任务。
- [x] `02:00` 主检正式线路，并增量验证 GitHub 新候选。
- [x] `13:00` 只复检正式线路；不重新扫描全部候选。
- [x] 保留单线程、低优先级、运行时间上限、内存/负载保护、原子状态文件和大面积失败熔断。
- [x] 将未跑完的候选保存为队列，下一天续跑，避免为了赶完而压垮 AC86U。

阶段测试：两时段调度、候选队列、候选/正式隔离和安装脚本共 21 项相关测试全部通过；全仓库共 171 项，154 项通过，仍只有基线记录的 6 项失败和 11 项报错。AC86U 每条正式线路仍为两个 2 MiB 上限分片；候选使用两个 6 MiB 上限分片并读取元数据。处理数量不设上限，达到 3300 秒资源预算时把余项原子保存在 U 盘，第二天续跑。

### 3. 家庭唯一裁决与替换引擎（已完成）

- [x] 当前线路同一轮连续失败且家庭网络基线正常后，才允许判定不合格。
- [x] 候选必须通过连续视频分片、下载余量、真实分辨率/编码和最低码率检查。
- [x] 正式切换前快速复验家庭备用，过期备用不得直接使用。
- [x] 单份家庭报告可产生不限频道数的替换决定；路由器本身不修改正式清单。
- [x] 无合格备用时只记录待处理，不使用香港或 GitHub 云端存活结果兜底。
- [x] 家庭备份有效期为 36 小时；`02:00` 优先复检将在 18 小时内到期的备份，GitHub 候选拉取失败也不影响本地备份维护。

阶段测试：家庭契约、裁决、探针和安装相关 32 项全部通过。全仓库共 182 项，165 项通过，仍只有基线记录的 6 项失败和 11 项报错，没有新增回归。当前正式线路第一次检测不合格后必须在同一家庭路径完整复测；一次失败后恢复只记为 `UNKNOWN`，两次都不合格才记为 `BAD`。测试模拟 8 台同时不合格时生成 8 项替换决定，证明没有“最多 6 台”限制；模拟 12 台大面积同时异常时整轮熔断且替换数为 0。好线路即使存在评分更高的备份也保持不动，备份切换前复验失败会立即移出池。

### 4. AC86U 直连 GitHub（已完成）

- [x] 路由器通过现有家庭分流主动连接 GitHub，不开放家庭入站端口。
- [x] 使用仅限本仓库、可单独撤销的写入凭据；不保存账号级令牌。
- [x] 路由器只提交家庭报告/决策，正式分支留给下一阶段的受保护 GitHub 工作流发布。
- [x] GitHub 连接失败时在 U 盘保留本地报告并延后重试，正式清单保持不变。

阶段结果：远端已建立只含说明文件的独立 `home-reports` 分支（初始提交 `2bad257c7caf7a4939184527d0701837a4016431`）。AC86U 使用仓库专用 Ed25519 Deploy Key，经 `ssh.github.com:443` 主动推送；GitHub 官方主机指纹、仓库名和报告分支均锁死，不接受账号令牌、未知主机密钥或其他目标。待发报告入队和推送前都会校验版本、探针编号、大小、内容哈希与文件名，失败或并发冲突时保留 U 盘队列并从新克隆重试。Deploy Key 本身是仓库级而不是分支级，因此 `protected_publishing_ready=false` 是硬停机销：Phase 5 未完成默认分支保护和发布器前，即使误开推送也会失败关闭。

阶段专项测试 10 项全部通过；全仓库共 188 项，171 项通过，仍为基线已有的 6 项失败和 11 项报错，没有新增回归。

### 5. GitHub 采集与发布工作流（已完成，默认停用）

- [x] 每日采集在 `02:00` 家庭主检前完成，并生成只含新增/变更项的候选清单。
- [x] 发布工作流严格校验报告来源、版本、时间、正式清单哈希和家庭路径标记。
- [x] 只接受家庭已确认的替换决定，并同步更新 `tv.m3u`、`tv-all.m3u`、`tv-core.m3u`、`tv-easy.m3u`。
- [x] 发布冲突、过期报告或清单版本变化时拒绝修改并等待下一轮家庭检测。

阶段结果：GitHub 每天北京时间 `00:30` 只抓文本并生成新增/变更候选，绝不在云端给线路判定健康；家庭报告发布器在 `02:25` 和 `13:25` 读取隔离的 `home-reports` 分支。发布器要求报告文件名与内容哈希一致、探针编号匹配、报告不超过 18 小时、家庭路径与网络基线安全、正式 `tv-core.m3u` URL/哈希/频道集合/逐台当前 URL 完全一致，并且只接受 `BAD` 当前线路与切换前重新验证过的 `QUALIFIED` 备用。`UNKNOWN`、过期报告、并发版本变化、用户反馈否决和不支持的请求参数都会失败关闭。

四份清单按“频道键 + 报告中的旧 URL”精确替换，因此其他清单中本来健康的不同备用不会被误覆盖；同轮替换数没有上限，四份输出和审计回执在一个 Git 快照中发布，写回执失败会回滚全部清单。两个 GitHub 工作流都只建立 `automation/...` 临时分支，再创建并 squash 合并 PR，不再直推 `master`。AC86U 与工作流使用公开 GitHub Rules API 核验同一条仓库 Ruleset 同时要求 PR、0 人工审批、禁止删除和强推；Ruleset 绕过名单必须在部署时保持为空。

阶段专项测试 40 项全部通过；全仓库共 209 项，192 项通过，仍只有基线的 6 项失败和 11 项报错。真实四份清单结构检查通过，`tv-core.m3u` 含 53 个唯一受管频道；对仓库执行发布器返回 `disabled`，四份正式清单哈希未改变。`config/home-publisher.json` 继续保持 `enabled=false` 且探针编号为空，待 Phase 7 现场取得真实编号、规则集和影子报告均验收后才可单独启用。

### 6. 移除旧香港和轮换生产入口（已完成）

- [x] 停用香港每 6 小时探测与自动故障切换工作流。
- [x] 移除家庭报告经香港 SSH 接收器中转的依赖。
- [x] 停用三天轮换配置和入口。
- [x] 在新链路完成影子验证前，旧文件只隔离、不提前删除，避免失去回退依据。

阶段结果：旧每 6 小时 GitHub 采集和三天轮换工作流均已移除定时入口、降为只读并硬停用；香港探针及家庭报告接收器安装器会在执行任何安装动作前退出。已经安装的香港周期脚本取得新版本后，会先停用自身 systemd timer 或 cron，再在探测、上传、故障切换和生产写入前退出。AC86U 升级安装时会把旧香港接收器脚本和 SSH 文件改名隔离，不再安装或使用它们。旧实现仍保留在不可达代码中，只作为回退依据。

阶段专项测试 37 项全部通过，Shell 语法和两份退役工作流 YAML 均通过解析。全仓库共 209 项，192 项通过，仍只有基线的 6 项失败和 11 项报错，没有新增回归。四份正式播放清单与阶段开始时完全一致。

### 7. 测试、影子运行与现场部署

- [x] 单元测试覆盖 02:00、13:00、候选续跑、家庭断网、GitHub 失败、清单竞争更新和不限台替换。
- [x] 集成测试证明香港完全离线时系统仍能正常工作。
- [x] 在模拟 AC86U 环境运行至少两轮主检和一轮复检，不修改正式清单。
- [ ] U 盘到货、用户出院回家后安装 Entware 和探针。
- [ ] 现场核对 AC86U 与 Apple TV 的 DNS、出口 IP、分流策略及 IPv4/IPv6 路径一致。
- [ ] 完成家庭影子期后再开启正式发布。

当前结果：使用真实 `tv-core.m3u` 的 53 台范围完成 `02:00 → 13:00 → 次日 02:00` 模拟；两次主检验证候选，一次复检不扫描候选，三份报告从 AC86U 模拟端直接进入独立 `home-reports` Git 分支，不调用香港主机、接收器或香港健康数据。受保护发布器成功读取最新报告并返回 `shadow`，四份模拟台单和仓库内四份正式台单均逐字节不变。

新增贯通测试 1 项通过。全仓库共 210 项，193 项通过，仍只有基线的 6 项失败和 11 项报错，没有新增回归。余下三项必须等用户回家和 U 盘插入 AC86U 后现场完成；在此之前保持 `config/home-publisher.json` 的 `enabled=false`，不碰生产台单。

## 续做规则

1. 每次开始先读取本文件和分支最新提交，不依赖聊天记忆猜进度。
2. 每完成一个阶段就勾选对应项目、记录测试命令和结果，并单独提交。
3. 自动采集若继续更新 `master`，先把变更合并到本分支，再进入下一阶段。
4. 未通过阶段验收时，不进入下一阶段的生产启用动作。
5. 路由器未现场验证前，所有仓库代码只能处于未激活或影子模式。
