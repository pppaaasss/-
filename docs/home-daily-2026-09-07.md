# 家庭每日调度接通记录

## 已完成的现场结果

家庭试跑 COMPLETE，21 轮，候选剩余 0，备用池 105 个地址、42 个频道。
53 个当前频道中 29 GOOD、18 BAD、6 UNKNOWN。
PR #7 已合并至 master（1c80a0c3cc2e15e1e5615b53b108c8f65885f3de），
应用 11 个频道、42 行精确地址替换。深圳和河南的复测候选命中家庭反馈主机否决，未发布。

## 新调度实现

- 02:00：候选批次持续排空，再做当前源和备用复核。新一天任务保存在独立文件中，旧任务不被删除。
- 13:00：只检测当前源，使用有效的凌晨或晚高峰备用缓存，不扫描新候选。
- 20:00：晚高峰当前源复核及必要的备用切换复测；没有把普通凌晨检测称作高峰证据。
- 每 5 分钟恢复检查：只唤醒调度器，不等于每 5 分钟重新测速；没有待办任务就退出。
- 内存低于 50 MiB 保存批次，恢复到至少 58 MiB 再继续。CPU 85%、I/O wait 20% 保护保留。
- 单批仍最多 20 分钟，用于资源交接。总任务没有完成时限或轮数上限，允许跨日。
- 独占 flock 保证一个正式工作进程；中午和晚高峰任务在批次边界优先处理。
- 当前频道和替换复测有任务内检查点。当前采样缓存最多 6 小时，备用切换复测缓存最多 1 小时；过旧重新测量，而不是放宽质量标准。
- 单地址超时保留。网络或清单失败记录等待重试；Python 启动异常和信号崩溃记录错误、采集诊断并停止，不能冒充锁冲突。
- 晚高峰确认 BAD 按频道和 URL 保存，非高峰 GOOD 不会清除。仅同地址晚高峰 GOOD 可清除；已换新地址不继承旧地址 BAD。
- 家庭反馈的重复失败主机现在也在路由端筛除，避免先入池后被发布器拒绝。
- 原 pipeline-trial 目录保留。105 条试跑备用只导入正式待复核队列，不伪造正式合格证据，也不重测历史几千条失败候选。

## 接通状态与仍需现场完成的事项

代码实现不代表现场已经启用。安装器默认 daily_worker_enabled=false；不自行确认 Apple TV 分流、不启用 GitHub 推送和自动发布。

2026-09-07 读取 GitHub 仓库规则集为 []。现有 github_pair.py 与候选工作流要求 master 规则集具备 pull_request、deletion、non_fast_forward。
当前连接没有规则集或 Deploy Key 管理工具，因此仍需仓库所有者在设置页面完成规则集和路由器公钥配置。

部署入口 daily_from_termux.sh 从指定提交在手机暂存程序后安装，安装结束打印只含公开信息的 daily_readiness.py 报告。
已有 Entware 程序齐全时不重复重装 Python。安装不会启动未验证家庭路径的任务。

接下来依次核对路由器公钥和分流、完成 GitHub 配对、启用 shadow 回传、满足 activate.py 既有正式验收条件，再启用发布器。
现有正式验收仍要求 4 份跨至少 18 小时的成功回传报告；未擅自移除该条件。
Python 历史 frozen-import 异常根因尚未由现场诊断证据锁定；重装成功和这轮完成不能证明根因已解决。

## 验证

专项测试覆盖跨日保留、任务优先级、无轮数上限、资源暂停后的当前频道续测、晚高峰 BAD 保留、窗口外与熔断不制造高峰失败、报告契约与 GitHub 回传。
测试结果记录在提交/PR 中；尚无这版每日调度的路由端实跑证据。
